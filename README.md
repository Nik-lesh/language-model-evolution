# Language Model Evolution: RNN → LSTM → Transformer

Comparing three generations of language models trained on the same text corpus.

## Models
- Simple RNN (baseline)
- LSTM (improved)
- Transformer (state-of-the-art)

## Dataset
[Book name] by [Author]

## Results
[Will add after training]

